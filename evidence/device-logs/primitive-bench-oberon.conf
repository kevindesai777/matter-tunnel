# Measurement overlay: on-device crypto benchmark.
# The Ed25519 PSA configuration now lives in the application's prj.conf, since
# it is the chosen algorithm rather than a measurement-only option.
CONFIG_TUNNEL_CRYPTO_BENCHMARK=y
